<x-layouts.app :title="config('app.name').' Stock Adjustment Detail'">
    <x-app-shell :page-title="$adjustment->adjustment_number" section-label="Stock Adjustment Detail">
        <x-slot:actions><a href="{{ route('inventory.adjustments.index') }}" class="btn-secondary">Back to Adjustments</a><a href="{{ route('inventory.batches.index') }}" class="btn-primary">Inventory Batches</a></x-slot>
        <div class="space-y-5"><section class="surface-panel p-5"><div class="flex flex-col gap-3 border-b border-slate-200 pb-4 sm:flex-row sm:items-start sm:justify-between"><div><p class="section-kicker">Immutable inventory correction</p><h1 class="mt-1 text-2xl font-semibold text-ink-950">{{ $adjustment->adjustment_number }}</h1><p class="mt-2 text-sm text-slate-600">{{ $adjustment->reason }} / {{ $adjustment->adjustment_date?->format('d M Y') }}</p></div><span class="badge bg-medical-50 text-medical-700">{{ ucfirst($adjustment->status) }}</span></div><dl class="mt-5 grid gap-4 md:grid-cols-3"><div class="rounded-lg border border-slate-200 bg-slate-50/70 p-4"><dt class="field-label">Finalized</dt><dd class="mt-2 font-semibold text-ink-950">{{ $adjustment->finalized_at?->format('d M Y H:i') }}</dd></div><div class="rounded-lg border border-slate-200 bg-slate-50/70 p-4"><dt class="field-label">Changed Lines</dt><dd class="mt-2 font-semibold text-ink-950">{{ $adjustment->items->count() }}</dd></div><div class="rounded-lg border border-slate-200 bg-slate-50/70 p-4"><dt class="field-label">Accounting</dt><dd class="mt-2 font-semibold text-ink-950">{{ $adjustment->journalEntry?->entry_number ?: 'No value journal' }}</dd></div></dl></section><section class="surface-panel overflow-hidden"><div class="border-b border-slate-200 bg-white p-5"><p class="section-kicker">Count reconciliation</p><h2 class="mt-1 text-lg font-semibold text-ink-950">Before and after quantities</h2></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-left text-sm"><thead class="table-header"><tr><th class="px-4 py-3">Product / Batch</th><th class="px-4 py-3">Before</th><th class="px-4 py-3">Counted</th><th class="px-4 py-3">Difference</th><th class="px-4 py-3 text-right">Value</th></tr></thead><tbody class="divide-y divide-slate-100 bg-white">@foreach ($adjustment->items as $item)<tr><td class="px-4 py-3"><p class="font-semibold text-ink-950">{{ $item->product_name_snapshot }}</p><p class="mt-1 text-xs text-slate-500">{{ $item->batch_number_snapshot }}</p></td><td class="px-4 py-3 text-slate-700">{{ $item->before_quantity }}</td><td class="px-4 py-3 text-slate-700">{{ $item->counted_quantity }}</td><td class="px-4 py-3 font-semibold {{ str_starts_with($item->delta_quantity, '-') ? 'text-red-700' : 'text-medical-700' }}">{{ $item->delta_quantity }}</td><td class="px-4 py-3 text-right font-semibold text-ink-900">{{ $item->value_amount }}</td></tr>@endforeach</tbody></table></div></section>@if ($adjustment->journalEntry)<section class="surface-panel p-5"><p class="section-kicker">Linked Accounting</p><h2 class="mt-1 text-lg font-semibold text-ink-950">Inventory variance journal</h2><a href="{{ route('accounting.journal.show', $adjustment->journalEntry) }}" class="btn-secondary mt-4">{{ $adjustment->journalEntry->entry_number }}</a></section>@endif@if ($adjustment->notes)<section class="surface-panel p-5"><p class="section-kicker">Notes</p><p class="mt-2 text-sm text-slate-700">{{ $adjustment->notes }}</p></section>@endif</div>
    </x-app-shell>
</x-layouts.app>
