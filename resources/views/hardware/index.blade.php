<x-layouts.app :title="config('app.name').' Hardware'"><x-app-shell page-title="Hardware" section-label="Workstation Devices"><x-slot:actions><a href="{{ route('settings.index') }}" class="btn-secondary">Settings</a><a href="{{ route('status') }}" class="btn-primary">System Status</a></x-slot><div class="space-y-5">@if (session('status'))<div class="surface-panel border-medical-100 bg-medical-50 px-4 py-3 text-sm font-semibold text-medical-800">{{ session('status') }}</div>@endif @if (session('error'))<div class="surface-panel border-red-100 bg-red-50 px-4 py-3 text-sm font-semibold text-red-800">{{ session('error') }}</div>@endif <livewire:hardware-status-panel /></div></x-app-shell></x-layouts.app>
