<x-layouts.app :title="config('app.name').' Supplier Payment Detail'">
    <x-app-shell :page-title="$payment->payment_number" section-label="Supplier Payment Detail">
        <x-slot:actions><a href="{{ route('supplier-payments.index', $payment->supplier) }}" class="btn-secondary">Back to Payments</a><a href="{{ route('suppliers.ledger', $payment->supplier) }}" class="btn-secondary">Supplier Ledger</a><a href="{{ route('accounting.index') }}" class="btn-primary">Accounting</a></x-slot>
        <div class="space-y-5"><section class="surface-panel p-5"><div class="flex flex-col gap-3 border-b border-slate-200 pb-4 sm:flex-row sm:items-start sm:justify-between"><div><p class="section-kicker">Immutable supplier payment</p><h1 class="mt-1 text-2xl font-semibold text-ink-950">{{ $payment->payment_number }}</h1><p class="mt-2 text-sm text-slate-600">{{ $payment->supplier?->name }}</p></div><span class="badge bg-medical-50 text-medical-700">{{ ucfirst($payment->status) }}</span></div><dl class="mt-5 grid gap-4 md:grid-cols-4"><div class="rounded-lg border border-slate-200 bg-slate-50/70 p-4"><dt class="field-label">Payment Date</dt><dd class="mt-2 font-semibold text-ink-950">{{ $payment->payment_date?->format('d M Y') }}</dd></div><div class="rounded-lg border border-slate-200 bg-slate-50/70 p-4"><dt class="field-label">Method</dt><dd class="mt-2 font-semibold capitalize text-ink-950">{{ str_replace('_', ' ', $payment->payment_method) }}</dd></div><div class="rounded-lg border border-slate-200 bg-slate-50/70 p-4"><dt class="field-label">Amount</dt><dd class="mt-2 font-semibold text-alert-700">{{ $payment->amount }}</dd></div><div class="rounded-lg border border-slate-200 bg-slate-50/70 p-4"><dt class="field-label">Reference</dt><dd class="mt-2 font-semibold text-ink-950">{{ $payment->reference ?: 'Not set' }}</dd></div></dl></section><section class="surface-panel p-5"><p class="section-kicker">Linked Accounting</p><h2 class="mt-1 text-lg font-semibold text-ink-950">Supplier payable settlement</h2><p class="mt-2 text-sm text-slate-600">The supplier payable is debited and the selected cash or bank account is credited.</p>@if ($payment->journalEntry)<a href="{{ route('accounting.journal.show', $payment->journalEntry) }}" class="btn-secondary mt-4">{{ $payment->journalEntry->entry_number }}</a>@endif@if ($payment->notes)<p class="mt-5 border-t border-slate-200 pt-4 text-sm text-slate-700">{{ $payment->notes }}</p>@endif</section></div>
    </x-app-shell>
</x-layouts.app>
